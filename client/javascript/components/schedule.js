var React = require("react");

var Schedule = React.createClass({
    render: function() {
        return (
            <div className='schedule'>
                TODO: add tide schedule content
            </div>
        );
    }
});

module.exports = Schedule;